# Experiments

A sandbox for trying things out.

Experiments are managed through **lanes** — one folder per experiment under [`lanes/`](./lanes/), each with a status (candidate / in-progress / done / dropped). See [lanes/README.md](./lanes/README.md) for the model and the current list.
